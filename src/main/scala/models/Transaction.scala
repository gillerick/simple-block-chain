package io.iohk.atala.swetest
package models

/**
 * A simple case class that represents data stored in the mini blockchain
 *
 * @param data The string message to be stored
 */
case class Transaction(data: String)
